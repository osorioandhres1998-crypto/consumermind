'use client';

// Validator standalone (hub) — sin proyecto, entrada manual.
import ValidatorTool from '../../components/tools/ValidatorTool';

export default function ValidatorStandalonePage() {
  return <ValidatorTool />;
}
