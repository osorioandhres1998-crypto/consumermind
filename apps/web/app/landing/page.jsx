'use client';

// Landing Analyzer standalone (hub) — URL manual, sin proyecto.
import LandingTool from '../../components/tools/LandingTool';

export default function LandingStandalonePage() {
  return <LandingTool />;
}
